{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "/bin/sh -c \"exec uvicorn server:app --host 0.0.0.0 --port $PORT\"",
    "healthcheckPath": "/api/healthz"
  }
}
