{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "/bin/sh -c 'if [ \"$DEMO_SEED_ENABLED\" = \"true\" ]; then echo \"[PLATIA] Demo bootstrap enabled\"; python demo_showcase_seed.py; python demo_showcase_patch.py; else echo \"[PLATIA] Production mode: demo bootstrap disabled\"; fi; exec uvicorn server:app --host 0.0.0.0 --port $PORT --log-level info'",
    "healthcheckPath": "/api/",
    "healthcheckTimeout": 300
  }
}
