import { useCallback, useEffect, useRef, useState } from "react";
import { Download, ImagePlus, MoreHorizontal, Package, Pencil, Plus, Search, Trash2, Upload, WandSparkles, X } from "lucide-react";
import { toast } from "sonner";
import api, { apiError, downloadCsv } from "../lib/api";
import { useAuth } from "../context/AuthContext";
import { fmtBs, fmtMoney, fmtNum, stockStatus } from "../lib/format";
import { Button } from "../components/ui/button";
import { Input } from "../components/ui/input";
import { Label } from "../components/ui/label";
import { Dialog, DialogContent, DialogHeader, DialogTitle } from "../components/ui/dialog";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "../components/ui/select";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "../components/ui/dropdown-menu";
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle } from "../components/ui/alert-dialog";

const EMPTY={name:"",sku:"",barcode:"",category:"",brand:"",supplier:"",purchase_price:"",sale_price:"",stock:"",min_stock:"5",max_stock:"",unit:"unidad",image_url:null};
export default function Productos(){
 const{business,isAdmin}=useAuth();const currency=business?.currency||"USD";const[rate,setRate]=useState(null),[products,setProducts]=useState(null),[search,setSearch]=useState(""),[category,setCategory]=useState("todas"),[dialogOpen,setDialogOpen]=useState(false),[editing,setEditing]=useState(null),[form,setForm]=useState(EMPTY),[toDelete,setToDelete]=useState(null),[saving,setSaving]=useState(false),[imageLoading,setImageLoading]=useState(false),[imageResults,setImageResults]=useState([]);const fileRef=useRef(null);
 const load=useCallback(()=>{const params={};if(search)params.search=search;if(category!=="todas")params.category=category;api.get("/products",{params}).then(r=>setProducts(r.data.products)).catch(e=>toast.error(apiError(e)))},[search,category]);
 useEffect(()=>{const t=setTimeout(load,250);return()=>clearTimeout(t)},[load]);useEffect(()=>{api.get("/rates/current").then(r=>setRate(r.data.rate)).catch(()=>{})},[]);
 const categories=[...new Set((products||[]).map(p=>p.category).filter(Boolean))];
 const openCreate=()=>{setEditing(null);setForm(EMPTY);setImageResults([]);setDialogOpen(true)};
 const openEdit=p=>{setEditing(p);setImageResults([]);setForm({name:p.name,sku:p.sku||"",barcode:p.barcode||"",category:p.category||"",brand:p.brand||"",supplier:p.supplier||"",purchase_price:p.purchase_price,sale_price:p.sale_price,stock:p.stock,min_stock:p.min_stock,max_stock:p.max_stock??"",unit:p.unit||"unidad",image_url:p.image_url||null});setDialogOpen(true)};
 const readImage=file=>{if(!file)return;if(!["image/jpeg","image/png","image/webp"].includes(file.type))return toast.error("Usa JPG, PNG o WEBP");if(file.size>2*1024*1024)return toast.error("La imagen debe pesar menos de 2 MB");const r=new FileReader();r.onload=()=>setForm(f=>({...f,image_url:String(r.result)}));r.readAsDataURL(file)};
 const searchImages=async()=>{if(!form.name.trim())return toast.warning("Escribe primero el nombre del producto");setImageLoading(true);try{const r=await api.get("/products/image-search",{params:{q:form.name}});setImageResults(r.data.images||[]);if(!r.data.images?.length)toast.info("No encontramos imágenes para este producto")}catch(e){toast.error(apiError(e))}finally{setImageLoading(false)}};
 const save=async e=>{e.preventDefault();setSaving(true);const payload={...form,sku:form.sku||null,barcode:form.barcode||null,category:form.category||null,brand:form.brand||null,supplier:form.supplier||null,max_stock:form.max_stock===""?null:Number(form.max_stock),purchase_price:Number(form.purchase_price)||0,sale_price:Number(form.sale_price)||0,stock:Number(form.stock)||0,min_stock:Number(form.min_stock)||0,image_url:form.image_url||null};try{if(editing)await api.put(`/products/${editing.id}`,payload);else await api.post("/products",payload);toast.success(editing?"Producto actualizado":"Producto creado");setDialogOpen(false);load()}catch(err){toast.error(apiError(err))}finally{setSaving(false)}};
 const doDelete=async()=>{try{await api.delete(`/products/${toDelete.id}`);toast.success(`"${toDelete.name}" eliminado`);setToDelete(null);load()}catch(err){toast.error(apiError(err))}};
 const doImport=async e=>{const file=e.target.files?.[0];if(!file)return;const fd=new FormData();fd.append("file",file);try{const{data}=await api.post("/products/import",fd);toast.success(`${data.created} producto(s) importado(s)`);load()}catch(err){toast.error(apiError(err,"No pudimos importar el archivo."))}finally{e.target.value=""}};
 const field=(key,label,props={})=><div className="space-y-1.5"><Label>{label}</Label><Input data-testid={`product-form-${key}`} value={form[key]??""} onChange={e=>setForm({...form,[key]:e.target.value})} {...props}/></div>;
 return <div className="space-y-5" data-testid="productos-page"><div className="flex items-center justify-between flex-wrap gap-3"><div><h1 className="font-heading text-3xl font-extrabold tracking-tight">Productos</h1><p className="text-sm text-muted-foreground mt-1">Catálogo, precios, stock e imágenes.</p></div>{isAdmin&&<div className="flex gap-2 flex-wrap"><input ref={fileRef} type="file" accept=".csv" className="hidden" onChange={doImport}/><Button variant="outline" onClick={()=>fileRef.current?.click()} className="rounded-xl"><Upload className="w-4 h-4 mr-1.5"/>Importar CSV</Button><Button variant="outline" onClick={()=>downloadCsv("/products/export/csv","productos.csv")} className="rounded-xl"><Download className="w-4 h-4 mr-1.5"/>Exportar CSV</Button><Button onClick={openCreate} className="rounded-xl"><Plus className="w-4 h-4 mr-1.5"/>Nuevo producto</Button></div>}</div><div className="flex gap-3 flex-wrap"><div className="relative flex-1 min-w-56"><Search className="w-4 h-4 absolute left-3.5 top-1/2 -translate-y-1/2 text-muted-foreground"/><Input className="pl-10 rounded-xl" placeholder="Buscar por nombre, código o SKU…" value={search} onChange={e=>setSearch(e.target.value)}/></div><Select value={category} onValueChange={setCategory}><SelectTrigger className="w-52 rounded-xl"><SelectValue placeholder="Categoría"/></SelectTrigger><SelectContent><SelectItem value="todas">Todas las categorías</SelectItem>{categories.map(c=><SelectItem key={c} value={c}>{c}</SelectItem>)}</SelectContent></Select></div><div className="bg-card border rounded-2xl overflow-hidden">{!products?<div className="p-6 space-y-3">{[...Array(5)].map((_,i)=><div key={i} className="h-10 bg-secondary rounded-xl animate-pulse"/>)}</div>:products.length===0?<div className="p-12 text-center"><Package className="w-10 h-10 text-muted-foreground mx-auto mb-3"/><p className="font-semibold">Aún no tienes productos</p><Button onClick={openCreate} className="mt-4 rounded-xl"><Plus className="w-4 h-4 mr-1.5"/>Crear producto</Button></div>:<div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="text-left text-xs uppercase tracking-wide text-muted-foreground border-b bg-secondary/50"><th className="px-5 py-3">Producto</th><th className="px-4 py-3">Categoría</th><th className="px-4 py-3 text-right">Precio</th><th className="px-4 py-3 text-right">Costo</th><th className="px-4 py-3 text-right">Stock</th><th className="px-4 py-3">Estado</th>{isAdmin&&<th/>}</tr></thead><tbody className="divide-y">{products.map(p=>{const st=stockStatus(p);return <tr key={p.id} className="hover:bg-secondary/40"><td className="px-5 py-3"><div className="flex items-center gap-3"><div className="w-12 h-12 rounded-lg bg-secondary overflow-hidden shrink-0"><img src={p.image_url||"/placeholder-product.svg"} onError={e=>e.currentTarget.src="/placeholder-product.svg"} className="w-full h-full object-cover" alt=""/></div><div><p className="font-medium">{p.name}</p><p className="text-xs text-muted-foreground">{p.sku}</p></div></div></td><td className="px-4 py-3">{p.category}</td><td className="px-4 py-3 text-right font-num font-semibold">{fmtMoney(p.sale_price,currency)}{rate&&<div className="text-[11px] text-muted-foreground">{fmtBs(p.sale_price*rate)}</div>}</td><td className="px-4 py-3 text-right font-num text-muted-foreground">{fmtMoney(p.purchase_price,currency)}</td><td className="px-4 py-3 text-right font-num">{fmtNum(p.stock)}</td><td className="px-4 py-3"><span className={`text-[11px] font-bold px-2 py-0.5 rounded-full border ${st.cls}`}>{st.label}</span></td>{isAdmin&&<td className="px-4 py-3 text-right"><DropdownMenu><DropdownMenuTrigger asChild><button className="p-1.5 rounded-lg hover:bg-secondary"><MoreHorizontal className="w-4 h-4"/></button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem onClick={()=>openEdit(p)}><Pencil className="w-3.5 h-3.5 mr-2"/>Editar</DropdownMenuItem><DropdownMenuItem onClick={()=>setToDelete(p)} className="text-rose-600"><Trash2 className="w-3.5 h-3.5 mr-2"/>Eliminar</DropdownMenuItem></DropdownMenuContent></DropdownMenu></td>}</tr>})}</tbody></table></div>}</div>
 <Dialog open={dialogOpen} onOpenChange={setDialogOpen}><DialogContent aria-describedby={undefined} className="max-w-2xl max-h-[92vh] overflow-y-auto"><DialogHeader><DialogTitle className="font-heading">{editing?"Editar producto":"Nuevo producto"}</DialogTitle></DialogHeader><form onSubmit={save} className="grid grid-cols-2 gap-3.5"><div className="col-span-2">{field("name","Nombre *",{required:true,placeholder:"Ej. Coca-Cola 1.5L"})}</div>{field("sku","Código / SKU")}{field("barcode","Código de barras")}{field("category","Categoría")}{field("brand","Marca")}{field("purchase_price","Costo de compra",{type:"number",min:"0",step:"any",required:true})}{field("sale_price","Precio de venta",{type:"number",min:"0",step:"any",required:true})}{!editing&&field("stock","Stock inicial",{type:"number",min:"0",step:"any"})}{field("min_stock","Stock mínimo",{type:"number",min:"0",step:"any"})}{field("supplier","Proveedor")}{field("unit","Unidad")}
 <div className="col-span-2 border rounded-2xl p-4 space-y-3"><div className="flex items-center justify-between"><div><Label className="text-base">Imagen del producto</Label><p className="text-xs text-muted-foreground mt-1">JPG, PNG o WEBP · máximo 2 MB</p></div><div className="flex gap-2"><Button type="button" variant="outline" size="sm" onClick={()=>fileRef.current?.click()}><ImagePlus className="w-4 h-4 mr-1.5"/>Cargar foto</Button><Button type="button" variant="outline" size="sm" onClick={searchImages} disabled={imageLoading}><WandSparkles className="w-4 h-4 mr-1.5"/>{imageLoading?"Buscando…":"Buscar imagen"}</Button></div></div><input type="file" accept="image/jpeg,image/png,image/webp" className="hidden" ref={fileRef} onChange={e=>{readImage(e.target.files?.[0]);e.target.value=""}}/>{form.image_url&&<div className="relative w-40 h-40 rounded-xl overflow-hidden border"><img src={form.image_url} className="w-full h-full object-cover" alt="Preview"/><button type="button" onClick={()=>setForm(f=>({...f,image_url:null}))} className="absolute top-2 right-2 bg-black/70 text-white rounded-full p-1"><X className="w-4 h-4"/></button></div>}{imageResults.length>0&&<div><p className="text-xs font-semibold mb-2">Selecciona una imagen</p><div className="grid grid-cols-4 gap-2">{imageResults.slice(0,8).map((im,i)=><button type="button" key={i} onClick={()=>{setForm(f=>({...f,image_url:im.url}));setImageResults([])}} className="rounded-lg overflow-hidden border hover:ring-2 hover:ring-primary"><img src={im.thumbnail||im.url} className="aspect-square w-full object-cover" alt={im.title||""}/></button>)}</div></div>}</div>
 <div className="col-span-2 flex justify-end gap-2 pt-2"><Button type="button" variant="ghost" onClick={()=>setDialogOpen(false)}>Cancelar</Button><Button type="submit" disabled={saving} className="rounded-xl">{saving?"Guardando…":editing?"Guardar cambios":"Crear producto"}</Button></div></form></DialogContent></Dialog>
 <AlertDialog open={!!toDelete} onOpenChange={()=>setToDelete(null)}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>¿Eliminar "{toDelete?.name}"?</AlertDialogTitle><AlertDialogDescription>El producto se quitará del catálogo. El historial de movimientos se conserva.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Cancelar</AlertDialogCancel><AlertDialogAction onClick={doDelete} className="bg-rose-600 hover:bg-rose-700">Eliminar</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog></div>;
}
