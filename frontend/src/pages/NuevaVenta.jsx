import {useCallback,useEffect,useMemo,useState} from "react";
import {Check,Minus,Package,Plus,Search,ShoppingCart,Trash2,X} from "lucide-react";
import {toast} from "sonner";
import api,{apiError} from "../lib/api";
import {useAuth} from "../context/AuthContext";
import {fmtBs,fmtMoney,fmtNum,PAYMENT_METHODS} from "../lib/format";
const UNITS=["unidad","kg","g","lb","l","ml","m","cm"];
const FACTOR={unidad:1,kg:1000,g:1,lb:453.592,l:1000,ml:1,m:100,cm:1};
const paymentPart=(method="efectivo")=>({method,amount:""});
export default function NuevaVenta(){
 const{business,rate}=useAuth();const[products,setProducts]=useState([]),[cart,setCart]=useState([]),[query,setQuery]=useState(""),[category,setCategory]=useState("Todas"),[paymentOpen,setPaymentOpen]=useState(false),[payment,setPayment]=useState("efectivo"),[split,setSplit]=useState(false),[parts,setParts]=useState([paymentPart("efectivo"),paymentPart("tarjeta")]),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false);
 const display=business?.display_currency||"dual";const subtotal=cart.reduce((s,x)=>s+Number(x.sale_price||0)*x.quantity,0);const count=cart.reduce((s,x)=>s+x.quantity,0);const paid=parts.reduce((s,p)=>s+(Number(p.amount)||0),0);const difference=Number((subtotal-paid).toFixed(2));
 const load=useCallback(async()=>{setLoading(true);try{const r=await api.get("/products");setProducts((r.data.products||[]).filter(p=>Number(p.stock||0)>0))}catch(e){toast.error(apiError(e))}finally{setLoading(false)}},[]);useEffect(()=>{load()},[load]);
 const cats=useMemo(()=>["Todas",...new Set(products.map(p=>p.category).filter(Boolean))],[products]);const visible=useMemo(()=>products.filter(p=>(category==="Todas"||p.category===category)&&`${p.name||""} ${p.sku||""} ${p.barcode||""}`.toLowerCase().includes(query.toLowerCase())),[products,query,category]);
 const price=n=>display==="Bs"?(rate?.rate?fmtBs(n*rate.rate):"Bs —"):display==="USD"?fmtMoney(n,"USD"):<><b>{fmtMoney(n,"USD")}</b>{rate?.rate&&<span className="block text-[10px] text-muted-foreground">{fmtBs(n*rate.rate)}</span>}</>;
 const add=(p,u=p.unit||p.base_unit||"unidad")=>{const base=p.base_unit||p.unit||"unidad",f=(FACTOR[u]||1)/(FACTOR[base]||1),q=cart.find(x=>x.id===p.id&&x.unit===u)?.quantity||0;if((q+1)*f>Number(p.stock||0)+1e-9)return toast.warning("No hay stock suficiente");setCart(c=>{const e=c.find(x=>x.id===p.id&&x.unit===u);return e?c.map(x=>x.id===p.id&&x.unit===u?{...x,quantity:x.quantity+1}:x):[...c,{...p,unit:u,base_unit:base,quantity:1}]})};
 const remove=(id,u)=>setCart(c=>c.map(x=>x.id===id&&x.unit===u?{...x,quantity:x.quantity-1}:x).filter(x=>x.quantity>0));
 const checkout=async()=>{if(!cart.length||saving)return;if(split&&Math.abs(difference)>.01)return toast.warning(`Falta cubrir ${fmtMoney(Math.max(difference,0))}`);setSaving(true);try{const r=await api.post("/sales",{items:cart.map(x=>({product_id:x.id,quantity:x.quantity,unit:x.unit,unit_price:Number(x.sale_price||0),discount:0})),payment_method:split?"combinado":payment,payment_parts:split?parts.filter(p=>Number(p.amount)>0).map(p=>({method:p.method,amount:Number(p.amount)})):[]});toast.success(`Venta ${r.data.sale?.invoice_number||""} registrada correctamente`);setCart([]);setPaymentOpen(false);setSplit(false);setParts([paymentPart("efectivo"),paymentPart("tarjeta")]);await load()}catch(e){toast.error(apiError(e))}finally{setSaving(false)}};
 const Card=({p})=>{const u=p.unit||p.base_unit||"unidad",q=cart.find(x=>x.id===p.id&&x.unit===u)?.quantity||0,agotado=q>=Number(p.stock);return <article className={`bg-card border rounded-xl overflow-hidden ${q?"ring-2 ring-primary":""}`}><button className="w-full text-left" onClick={()=>add(p)} disabled={agotado}><div className="aspect-square bg-secondary relative"><img src={p.image_url||"/placeholder-product.svg"} onError={e=>e.currentTarget.src="/placeholder-product.svg"} alt={p.name} className="w-full h-full object-cover"/><span className="absolute top-1 left-1 bg-background/90 rounded px-1 py-0.5 text-[9px] font-bold">{fmtNum(Math.max(Number(p.stock)-q,0))}</span>{q>0&&<span className="absolute top-1 right-1 bg-primary text-white rounded-full w-6 h-6 flex items-center justify-center text-[10px] font-bold">{q}</span>}</div><div className="p-2"><p className="font-semibold text-xs truncate">{p.name}</p><p className="text-[11px] font-bold mt-1">{price(Number(p.sale_price||0))}</p></div></button><div className="px-2 pb-2">{agotado?<div className="h-7 rounded-lg bg-secondary text-center text-[11px] font-bold py-1.5">Agotado</div>:<div className="flex items-center justify-between border rounded-lg h-7"><button onClick={()=>remove(p.id,u)} className="w-9 h-full"><Minus className="w-3 mx-auto"/></button><b className="text-xs">{q}</b><button onClick={()=>add(p,u)} className="w-9 h-full"><Plus className="w-3 mx-auto"/></button></div>}</div></article>};
 const Cart=()=> <aside className="bg-background border rounded-xl flex flex-col overflow-hidden lg:w-[320px] xl:w-[350px] lg:sticky lg:top-24 lg:h-[calc(100vh-7rem)]"><div className="p-3 border-b flex items-center gap-2"><ShoppingCart className="w-5 h-5 text-primary"/><div><b>Carrito</b><p className="text-[11px] text-muted-foreground">{count} artículos</p></div></div><div className="flex-1 overflow-y-auto p-2 space-y-2">{cart.length?cart.map(x=><div key={`${x.id}-${x.unit}`} className="border rounded-lg p-2 flex gap-2"><img src={x.image_url||"/placeholder-product.svg"} className="w-10 h-10 rounded object-cover" alt=""/><div className="flex-1 min-w-0"><p className="text-xs font-semibold truncate">{x.name}</p><p className="text-[10px] text-muted-foreground">{x.quantity} {x.unit} · {price(Number(x.sale_price||0)*x.quantity)}</p><div className="flex items-center gap-1 mt-1"><button onClick={()=>remove(x.id,x.unit)} className="w-6 h-6 border rounded"><Minus className="w-3 mx-auto"/></button><b className="text-xs px-1">{x.quantity}</b><button onClick={()=>add(x,x.unit)} className="w-6 h-6 border rounded"><Plus className="w-3 mx-auto"/></button></div></div></div>):<div className="py-12 text-center text-muted-foreground"><ShoppingCart className="w-8 h-8 mx-auto mb-2"/><p className="text-sm font-semibold">Carrito vacío</p></div>}</div><div className="border-t p-3"><div className="flex justify-between text-sm"><span>Total</span><b>{price(subtotal)}</b></div><button disabled={!cart.length} onClick={()=>setPaymentOpen(true)} className="w-full h-10 mt-2 rounded-xl bg-primary text-white font-bold"><Check className="w-4 h-4 inline mr-1"/>PAGO</button><button disabled={!cart.length} onClick={()=>setCart([])} className="w-full h-7 mt-1 rounded-lg border text-xs font-semibold"><Trash2 className="w-3 h-3 inline mr-1"/>Vaciar</button></div></aside>;
 return <div className="space-y-4" data-testid="nueva-venta-page"><div><h1 className="font-heading text-2xl sm:text-3xl font-extrabold flex items-center gap-2"><ShoppingCart className="w-6 h-6 text-primary"/>Punto de venta</h1><p className="text-xs sm:text-sm text-muted-foreground mt-1">Productos compactos para aprovechar la pantalla.</p></div><div className="flex gap-3 items-start"><section className="flex-1 min-w-0"><div className="bg-card border rounded-xl p-3 sticky top-[72px] z-20"><div className="relative"><Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-muted-foreground"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar producto, SKU o código…" className="w-full h-10 rounded-lg border bg-background pl-9 text-sm"/></div><div className="flex gap-1.5 overflow-x-auto mt-2">{cats.map(c=><button key={c} onClick={()=>setCategory(c)} className={`px-2.5 h-8 rounded-lg border text-[11px] font-semibold whitespace-nowrap ${category===c?"bg-primary text-white":""}`}>{c}</button>)}</div></div>{loading?<div className="grid grid-cols-3 sm:grid-cols-4 xl:grid-cols-5 gap-2.5 mt-3">{Array.from({length:10}).map((_,i)=><div key={i} className="h-48 bg-secondary rounded-xl animate-pulse"/>)}</div>:visible.length?<div className="grid grid-cols-3 sm:grid-cols-4 xl:grid-cols-5 gap-2.5 mt-3">{visible.map(p=><Card key={p.id} p={p}/>)}</div>:<div className="p-10 text-center"><Package className="w-9 h-9 mx-auto text-muted-foreground"/><p className="font-semibold mt-2">No hay productos disponibles</p></div>}</section><Cart/></div><div className="lg:hidden"><p className="text-xs text-muted-foreground mb-1">Carrito</p><Cart/></div>
 {paymentOpen&&<div className="fixed inset-0 z-[80] bg-black/50 flex items-center justify-center p-4" onClick={()=>setPaymentOpen(false)}><div className="w-full max-w-md bg-background rounded-2xl border shadow-2xl p-5" onClick={e=>e.stopPropagation()}><div className="flex items-center mb-4"><div className="flex-1"><h2 className="font-heading text-xl font-extrabold">PAGO</h2><p className="text-xs text-muted-foreground">Total: {price(subtotal)}</p></div><button onClick={()=>setPaymentOpen(false)}><X/></button></div><div className="grid grid-cols-2 gap-2 mb-4"><button onClick={()=>setSplit(false)} className={`h-10 rounded-xl border text-sm font-bold ${!split?"bg-primary text-white":""}`}>Un medio</button><button onClick={()=>setSplit(true)} className={`h-10 rounded-xl border text-sm font-bold ${split?"bg-primary text-white":""}`}>Dividir pago</button></div>{!split?<select value={payment} onChange={e=>setPayment(e.target.value)} className="w-full h-11 rounded-xl border bg-background px-3 mb-4">{PAYMENT_METHODS.map(m=><option key={m.value} value={m.value}>{m.label}</option>)}</select>:<div className="space-y-2 mb-4">{parts.map((p,i)=><div key={i} className="grid grid-cols-[1fr_100px_30px] gap-2"><select value={p.method} onChange={e=>setParts(c=>c.map((x,j)=>j===i?{...x,method:e.target.value}:x))} className="h-10 rounded-lg border bg-background px-2 text-xs">{PAYMENT_METHODS.map(m=><option key={m.value} value={m.value}>{m.label}</option>)}</select><input type="number" min="0" step="0.01" value={p.amount} onChange={e=>setParts(c=>c.map((x,j)=>j===i?{...x,amount:e.target.value}:x))} placeholder="Monto" className="h-10 rounded-lg border px-2 text-sm"/><button type="button" disabled={parts.length<=2} onClick={()=>setParts(c=>c.filter((_,j)=>j!==i))} className="border rounded-lg"><X className="w-3 h-3 mx-auto"/></button></div>)}<button onClick={()=>setParts(c=>[...c,paymentPart()])} className="text-xs font-bold text-primary">+ Agregar otro medio</button><p className={`text-sm font-semibold ${Math.abs(difference)<=.01?"text-emerald-700":"text-amber-700"}`}>Pagado {fmtMoney(paid)} · {Math.abs(difference)<=.01?"Completo":`Falta ${fmtMoney(Math.max(difference,0))}`}</p></div>}<button onClick={checkout} disabled={saving||!cart.length||(split&&Math.abs(difference)>.01)} className="w-full h-11 rounded-xl bg-primary text-white font-bold">{saving?"Registrando…":"Confirmar pago y registrar venta"}</button></div></div>}</div>;
}
