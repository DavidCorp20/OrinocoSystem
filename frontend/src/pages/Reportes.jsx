import {useEffect,useState} from "react";
import {BarChart3,Download,FileText,Package,ShoppingCart,Truck,Wallet} from "lucide-react";
import {toast} from "sonner";import api,{downloadCsv} from "../lib/api";import {fmtMoney,fmtNum,fmtPct} from "../lib/format";import {Button} from "../components/ui/button";import {Input} from "../components/ui/input";import {Label} from "../components/ui/label";
const REPORTS=[{key:"ventas",title:"Ventas",desc:"Ventas, productos, pagos y rentabilidad.",icon:ShoppingCart,path:"/sales/export/csv",dated:true},{key:"compras",title:"Compras",desc:"Proveedores, cantidades y costos.",icon:Truck,path:"/purchases/export/csv",dated:true},{key:"movimientos",title:"Movimientos de inventario",desc:"Entradas y salidas del inventario.",icon:Package,path:"/movements/export/csv",dated:false},{key:"productos",title:"Productos e inventario",desc:"Catálogo, costos, precios y stock.",icon:Package,path:"/products/export/csv",dated:false},{key:"gastos",title:"Gastos",desc:"Gastos operativos por categoría.",icon:Wallet,path:"/expenses/export/csv",dated:true}];
export default function Reportes(){const[analysis,setAnalysis]=useState(null);const[range,setRange]=useState({});const[downloading,setDownloading]=useState("");const get=k=>range[k]||{from:"",to:""};const setR=(k,p)=>setRange({...range,[k]:{...get(k),...p}});useEffect(()=>{api.get("/reports/business-analysis").then(r=>setAnalysis(r.data)).catch(()=>{});},[]);const exportR=async r=>{setDownloading(r.key);try{const x=get(r.key),q=[];if(r.dated&&x.from)q.push(`from_date=${x.from}`);if(r.dated&&x.to)q.push(`to_date=${x.to}`);await downloadCsv(`${r.path}${q.length?`?${q.join("&")}`:""}`,`${r.key}.csv`);toast.success(`Reporte de ${r.title.toLowerCase()} descargado`);}catch{toast.error("No pudimos generar el reporte.");}finally{setDownloading("");}};return <div className="space-y-5" data-testid="reportes-page"><div><h1 className="font-heading text-3xl font-extrabold tracking-tight text-slate-900">Reportes</h1><p className="text-sm text-muted-foreground mt-1">Información operativa y un diagnóstico de rentabilidad de tu negocio.</p></div>{analysis&&<div className="bg-card border border-border rounded-2xl p-5" data-testid="business-analysis-card"><div className="flex items-start gap-3"><div className="w-11 h-11 rounded-xl bg-primary/10 flex items-center justify-center"><BarChart3 className="w-5 h-5 text-primary"/></div><div className="flex-1"><h2 className="font-heading text-xl font-bold">Análisis del negocio · últimos 30 días</h2><p className="text-sm mt-1 font-semibold">{analysis.verdict}</p><p className="text-sm text-muted-foreground mt-1">{analysis.recommendation}</p></div></div><div className="grid grid-cols-2 md:grid-cols-5 gap-3 mt-5">{[["Ventas",fmtNum(analysis.metrics.sales_count),""],["Ingresos",fmtMoney(analysis.metrics.revenue),""],["Ganancia bruta",fmtMoney(analysis.metrics.gross_profit),""],["Gastos",fmtMoney(analysis.metrics.operating_expenses),""],["Resultado neto",fmtMoney(analysis.metrics.net_profit),fmtPct(analysis.metrics.net_margin_percent) ]].map(([a,b,c])=><div key={a} className="bg-secondary/60 rounded-xl p-3"><p className="text-xs text-muted-foreground">{a}</p><p className="font-num font-bold mt-1">{b}</p>{c&&<p className="text-xs text-muted-foreground">{c}</p>}</div>)}</div></div>}<div className="grid grid-cols-1 md:grid-cols-2 gap-4">{REPORTS.map(r=><div key={r.key} className="bg-card border border-border rounded-2xl p-5"><div className="flex items-start gap-3"><div className="w-11 h-11 rounded-xl bg-primary/10 flex items-center justify-center"><r.icon className="w-5 h-5 text-primary"/></div><div><h3 className="font-heading font-bold">{r.title}</h3><p className="text-xs text-muted-foreground">{r.desc}</p></div></div>{r.dated&&<div className="grid grid-cols-2 gap-3 mt-4"><div><Label className="text-xs">Desde</Label><Input type="date" value={get(r.key).from} onChange={e=>setR(r.key,{from:e.target.value})}/></div><div><Label className="text-xs">Hasta</Label><Input type="date" value={get(r.key).to} onChange={e=>setR(r.key,{to:e.target.value})}/></div></div>}<Button onClick={()=>exportR(r)} disabled={downloading===r.key} className="w-full mt-4 rounded-xl" variant="outline"><Download className="w-4 h-4 mr-1.5"/>{downloading===r.key?"Generando…":"Exportar CSV"}</Button></div>)}</div></div>}
