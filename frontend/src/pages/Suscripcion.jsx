import {useEffect,useState} from "react";
import {CreditCard,CalendarDays,Clock3,Upload,RefreshCw} from "lucide-react";import {toast} from "sonner";import api,{apiError} from "../lib/api";import {fmtDate,fmtMoney,fmtNum} from "../lib/format";import {Button} from "../components/ui/button";import {Input} from "../components/ui/input";import {Label} from "../components/ui/label";
const METHODS=[{value:"pagomovil",label:"Pago Móvil"},{value:"transferencia",label:"Transferencia"},{value:"binance",label:"Binance"}];
export default function Suscripcion(){const[data,setData]=useState(null),[form,setForm]=useState({method:"transferencia",reference:"",notes:""}),[saving,setSaving]=useState(false),[confirming,setConfirming]=useState(false);const load=async()=>{try{const r=await api.get("/subscription/me");setData(r.data)}catch(e){toast.error(apiError(e))}};useEffect(()=>{load()},[]);if(!data)return <div className="p-8">Cargando suscripción…</div>;const sub=data.subscription,plan=data.plan,rate=Number(data.bcv_rate||0),amountUsd=Number(sub?.monthly_price_usd||0),amountBs=rate?amountUsd*rate:0;if(!sub)return <div className="space-y-5"><h1 className="text-3xl font-extrabold">MI PLAN</h1><div className="bg-card border rounded-2xl p-6"><p className="text-muted-foreground">Tu negocio todavía no tiene un plan asignado. Contacta al administrador de PLATIA.</p></div></div>;const submit=async()=>{if(!rate){toast.error("No hay una tasa vigente disponible para registrar el pago");return}setSaving(true);try{const r=await api.post("/subscription/payments",{subscription_id:sub.id,amount_usd:amountUsd,payment_method:form.method,reference:form.reference||null,notes:form.notes||null});toast.success(`Pago enviado. Total: Bs ${fmtNum(r.data.payment.amount_bs)}`);setForm({method:"transferencia",reference:"",notes:""});setConfirming(false);load()}catch(x){toast.error(apiError(x))}finally{setSaving(false)}};return <div className="space-y-5" data-testid="subscription-page"><div className="flex items-center justify-between"><div><h1 className="text-3xl font-extrabold flex items-center gap-2"><CreditCard className="w-7 h-7 text-primary"/>MI PLAN</h1><p className="text-sm text-muted-foreground">Consulta tu plan, monto y vencimiento.</p></div><Button variant="outline" onClick={load}><RefreshCw className="w-4 h-4 mr-2"/>Actualizar</Button></div><div className="grid md:grid-cols-3 gap-4"><div className="bg-card border rounded-2xl p-5"><p className="text-sm text-muted-foreground">Plan</p><p className="text-2xl font-bold mt-2">{plan?.name||sub.plan_name}</p></div><div className="bg-card border rounded-2xl p-5"><p className="text-sm text-muted-foreground">Valor mensual</p><p className="text-2xl font-bold mt-2">{fmtMoney(amountUsd,'USD')}</p><p className="text-xs text-muted-foreground">≈ Bs {fmtNum(amountBs)} usando la tasa vigente de PLATIA</p></div><div className="bg-card border rounded-2xl p-5"><p className="text-sm text-muted-foreground">Vencimiento</p><p className="text-2xl font-bold mt-2 flex items-center gap-2"><CalendarDays className="w-5 h-5"/>{sub.due_date?fmtDate(sub.due_date):'Sin fecha'}</p></div></div><div className="bg-card border rounded-2xl p-6"><h2 className="font-heading font-bold text-xl">Registrar pago</h2><p className="text-sm text-muted-foreground mt-1">PLATIA aplica automáticamente la tasa vigente. No debes introducir ninguna tasa.</p><div className="mt-5"><Label>Método de pago</Label><div className="grid grid-cols-3 gap-2 mt-2">{METHODS.map(m=><button key={m.value} type="button" onClick={()=>setForm({...form,method:m.value})} className={`rounded-xl border px-4 py-3 font-semibold transition ${form.method===m.value?'border-primary bg-primary/10 text-primary':'bg-background hover:bg-muted'}`}>{m.label}</button>)}</div></div><div className="mt-4"><Label>Referencia</Label><Input value={form.reference} onChange={e=>setForm({...form,reference:e.target.value})} placeholder="Número de referencia"/></div><div className="mt-4"><Label>Notas</Label><Input value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></div><div className="mt-5"><Button disabled={saving||!rate} onClick={()=>setConfirming(true)}><Upload className="w-4 h-4 mr-2"/>Registrar pago</Button></div></div><div className="bg-card border rounded-2xl overflow-hidden"><div className="px-5 py-4 border-b flex items-center gap-2"><Clock3 className="w-5 h-5"/><h2 className="font-heading font-bold">Historial de pagos</h2></div>{(data.payments||[]).map(p=><div key={p.id} className="px-5 py-4 border-b flex flex-wrap gap-3"><span className="flex-1">{fmtDate(p.paid_at)}</span><b>{fmtMoney(p.amount_usd||p.amount,'USD')}</b>{p.amount_bs&&<span>Bs {fmtNum(p.amount_bs)}</span>}<span className="text-sm">{p.status||'registrado'}</span></div>)}{!data.payments?.length&&<p className="p-6 text-sm text-muted-foreground">Aún no tienes pagos registrados.</p>}</div>{confirming&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4"><div className="w-full max-w-md rounded-2xl bg-card border shadow-xl p-6"><h3 className="text-xl font-bold">Confirmar registro de pago</h3><p className="mt-3 text-sm leading-6">El pago a cargar corresponde a <strong>{fmtMoney(amountUsd,'USD')}</strong> (Bs {fmtNum(amountBs)}), que es el monto que adeudas por tu plan según la tasa vigente de PLATIA.</p><p className="mt-2 text-sm text-muted-foreground">Método seleccionado: <strong>{METHODS.find(m=>m.value===form.method)?.label}</strong></p><div className="mt-6 flex justify-end gap-2"><Button variant="outline" disabled={saving} onClick={()=>setConfirming(false)}>Cancelar</Button><Button disabled={saving} onClick={submit}>{saving?'Registrando…':'Sí, registrar pago'}</Button></div></div></div>}</div>}
