import axios from "axios";
export const API_URL=`${process.env.REACT_APP_BACKEND_URL}/api`;
const api=axios.create({baseURL:API_URL,withCredentials:true});let refreshing=null;
api.interceptors.response.use(r=>r,async error=>{const original=error.config||{};if(error.response?.status===401&&!original._retry&&!(original.url||"").startsWith("/auth/")){original._retry=true;try{refreshing=refreshing||api.post("/auth/refresh");await refreshing;refreshing=null;return api(original)}catch(e){refreshing=null}}return Promise.reject(error)});
export function apiError(e,fallback="Ocurrió un error. Intenta de nuevo."){const d=e?.response?.data?.detail;if(typeof d==="string")return d;if(Array.isArray(d))return d.map(x=>x?.msg||"").filter(Boolean).join(" ");return fallback}
export async function downloadCsv(path,filename){const res=await api.get(path,{responseType:"blob"});const url=URL.createObjectURL(res.data);const a=document.createElement("a");a.href=url;a.download=filename;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url)}
export async function streamChat(message,onToken,onDone){const doFetch=()=>fetch(`${API_URL}/assistant/chat`,{method:"POST",credentials:"include",headers:{"Content-Type":"application/json","Accept":"text/event-stream"},body:JSON.stringify({message})});let res=await doFetch();if(res.status===401){const refresh=await fetch(`${API_URL}/auth/refresh`,{method:"POST",credentials:"include"});if(!refresh.ok)throw new Error("session expired");res=await doFetch()}if(!res.ok||!res.body){let detail="";try{detail=(await res.json()).detail||""}catch{}throw new Error(detail||`chat failed (${res.status})`)}const reader=res.body.getReader(),dec=new TextDecoder();let buf="",received=false,doneSignal=false;const consume=part=>{const line=part.trim();if(!line.startsWith("data:"))return;const payload=line.slice(5).trim();if(payload==="[DONE]"){doneSignal=true;return}const data=JSON.parse(payload);if(data.error)throw new Error(data.error);if(data.c){received=true;onToken(data.c)}};try{for(;;){const{done,value}=await reader.read();if(done)break;buf+=dec.decode(value,{stream:true});const parts=buf.split(/\r?\n\r?\n/);buf=parts.pop()||"";for(const part of parts)consume(part)}buf+=dec.decode();if(buf.trim())consume(buf)}finally{reader.releaseLock()}if(!received)throw new Error("El asistente no devolvió contenido");onDone?.(doneSignal)}
export default api;
