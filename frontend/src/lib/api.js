import axios from "axios";
export const API_URL=`${process.env.REACT_APP_BACKEND_URL}/api`;
const api=axios.create({baseURL:API_URL,withCredentials:true});let refreshing=null;
api.interceptors.response.use(r=>r,async error=>{const original=error.config||{};if(error.response?.status===401&&!original._retry&&!(original.url||"").startsWith("/auth/")){original._retry=true;try{refreshing=refreshing||api.post("/auth/refresh");await refreshing;refreshing=null;return api(original)}catch(e){refreshing=null}}return Promise.reject(error)});
export function apiError(e,fallback="Ocurrió un error. Intenta de nuevo."){const d=e?.response?.data?.detail;if(typeof d==="string")return d;if(Array.isArray(d))return d.map(x=>x?.msg||"").filter(Boolean).join(" ");if(typeof e?.message==="string"&&e.message)return e.message;return fallback}
export async function downloadCsv(path,filename){const res=await api.get(path,{responseType:"blob"});const url=URL.createObjectURL(res.data);const a=document.createElement("a");a.href=url;a.download=filename;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url)}
export async function streamChat(message,onToken,onDone){const doFetch=()=>fetch(`${API_URL}/assistant/chat`,{method:"POST",credentials:"include",headers:{"Content-Type":"application/json","Accept":"text/event-stream"},body:JSON.stringify({message})});let res=await doFetch();if(res.status===401){const refresh=await fetch(`${API_URL}/auth/refresh`,{method:"POST",credentials:"include"});if(!refresh.ok)throw new Error("La sesión expiró. Vuelve a iniciar sesión.");res=await doFetch()}if(!res.ok||!res.body){let detail="";try{detail=(await res.json()).detail||""}catch{}throw new Error(detail||`El asistente devolvió un error (${res.status})`)}const reader=res.body.getReader(),dec=new TextDecoder();let buf="",received=false,doneSignal=false,lastData=Date.now(),timer=setInterval(()=>{if(Date.now()-lastData>20000){try{reader.cancel()}catch{} }},1000);const consume=part=>{const line=part.trim();if(!line.startsWith("data:"))return;const payload=line.slice(5).trim();if(payload==="[DONE]"){doneSignal=true;return}let data;try{data=JSON.parse(payload)}catch{throw new Error("Respuesta inválida del asistente")}if(data.error&&data.c){received=true;onToken(data.c)}else if(data.error)throw new Error(typeof data.error==="string"?data.error:"El asistente encontró un error");if(data.c){received=true;onToken(data.c);lastData=Date.now()}};try{for(;;){if(Date.now()-lastData>20000)throw new Error("Pyme tardó demasiado en responder. Puedes reintentar.");const{done,value}=await reader.read();if(done)break;lastData=Date.now();buf+=dec.decode(value,{stream:true});const parts=buf.split(/\r?\n\r?\n/);buf=parts.pop()||"";for(const part of parts)consume(part)}buf+=dec.decode();if(buf.trim())consume(buf)}finally{clearInterval(timer);reader.releaseLock()}if(!received)throw new Error("Pyme no devolvió contenido. Puedes reintentar.");onDone?.(doneSignal)}
export default api;
